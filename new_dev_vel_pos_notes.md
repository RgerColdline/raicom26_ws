# 穿越赛段调优记录（new_dev_vel_pos 线）

> 最后更新：2026-09-17
> 涉及仓库：`raicom26_ws`（父）+ `raicom_vision_laser`（子模块）
> 当前工作分支：两边都是 `new_dev_vel_pos`

---

## 一、问题：为什么会撞柱子

**实机真正在飞的是 `main_control` 节点**（不是 `raicom_vision_laser` 里的 `traverse_node`）。

原方案：穿越段用**时间参数化的开环样条** —— `trackPlan()` 按"已过去的时间"去轨迹上采样，
发 **position-only** 设定点（掩码忽略 vx/vy/vz）。

问题链：

1. PX4 位置环是 P 控制，要维持速度 v 就必须靠位置误差"挣"出速度指令：`v ≈ Kp × err`。
   追一个移动目标时必然存在**稳态滞后** `err ≈ v / Kp ≈ 0.5 / 0.95 ≈ 0.5m`。
2. 滞后方向是**切向**（沿路径落后）。直线段只是"晚到"，不影响几何路径；
   但**弯道处切向会旋转**，滞后就变成**横向偏差 → 切内角**，把净距余量吃掉。
3. 9.17 实机 case1 撞柱实测：滞后 **0.4~0.75m**，最紧净距从计划 0.487 掉到 **0.44**（0.5s 采样，真实更低）。

**结论：调 `v_max/a_max/a_lat_max` 没用。** 那三个参数只改"参考轨迹跑多快"，
不改飞机实际速度（受 PX4 内部 `MPC_XY_VEL_MAX` 限制），多出来的全变成滞后误差。

---

## 二、走过的两条路

### 路线 A：折线逐点到点（已放弃，代码留在 master）

思路：穿越段不再走样条，改成"折线航点 + 全停到点"的闭环控制，从根上消掉滞后。

- 代码位置：`master` 分支（`raicom_vision_laser` 7058d13 / `raicom26_ws` 651a75b）
- yaml 键：`poly_go_caseX` / `poly_ret_caseX` + `traverse/use_polyline`
- 实测过 case1 斜摆，能过，但**放弃**了，改为统一到"速度前馈"路线。
- 折线期间踩到的坑（有参考价值）：
  - **position+velocity 混合掩码下，速度由 PX4 内部 `MPC_XY_P` 主导**，
    yaml 的 `p_xy` / `max_speed` 基本失效（把参数调到很低也没变慢）；
    改成 position-only 的 `positionControl` 纯速度控制后才受 yaml 控制。
  - 纯位置设定点 + P 控制有稳态残差，收紧到点容差会"爬"得极慢甚至卡住。

### 路线 B：时间参数化样条 + 切向速度前馈（当前方案）

保留时间参数化轨迹（动作平滑、时序可控），但**叠加切向前馈**，
让 PX4 不必靠误差"挣"速度：

```
最终速度指令 = 我们发的前馈 v_ff  +  PX4 的 Kp × 位置误差
```

掩码用 `TYPE_MASK_POSITION_VELOCITY`（position + velocity 同时生效）。

**第一版（均匀 ff_scale=0.7）实测**：
- 滞后从 0.4~0.75m 降到 **~0.25m** ✔
- 但拉到 `ff_scale=1.0` 反而更差：**末端过冲**（冲过终点 0.21m，多花 1.3s）+ 弯道切外圈
  （返程过柱净距 0.43 → 0.34，贴住膨胀圈）

**第二版（当前）：曲率自适应权重**

```
w_ff = 曲率因子 × 速度因子
  曲率因子 = 1 − v²κ / a_lat_max    直线=1 → 最紧弯=0
  速度因子 = min(1, v / v_max)      段中=1 → 段首/段尾≈0
ff_local = ff_curve + (ff_straight − ff_curve) × w_ff
```

- 依据（实测）：纯位置时弯道横偏 **0.35m**；ff=1.0 时切外圈 **0.13m**；**ff≈0.7 最优，仅 0.02m**
  → 弯道要降下来保贴合，直线可以给足消滞后
- 段首/段尾速度因子→0，位置环主导 → 消除末端过冲
- 参数：`traverse/ff_straight = 0.8`、`traverse/ff_curve = 0.6`、`ff_horizon = 0.10`
- 权重由规划器逐点算好写进轨迹点（`TrajPoint::w_ff`），运行时按时间插值

**顺带修掉的坑：z 前馈砸地弹跳**
z 前馈最初漏了"返程过环门控"——返程起点在射击区（场地 x≈1.22），
降高公式误判为已进入回家末段，差分出约 **−2.7 m/s 的垂直指令**：
飞机先砸到 −0.4m，再被位置环拉回、过冲到 **2.05m**。
现在 z 前馈与 `z_cmd` 共用同一道 `qt > t_ring` 门。

---

## 三、实测偏差规律（可用于调点）

数据源：最新仿真日志（case1，0.2s 采样，119 条）。

| 项 | 结果 |
|---|---|
| **切向滞后** | 恒定 **≈0.25m**（去程 −0.259 / 返程 −0.250），全程一致、不随弯道变化 |
| **横向偏差** | 平均 **≈0**（−0.010 / −0.005），**无系统性左/右偏**；局部可达 **±0.2** |
| **横偏最大的点** | **100% 落在弯道**；方向**偏向弯道内侧（切内角）** |
| 横偏 vs 转向 相关系数 | 去程 **+0.38**，返程 **+0.53**（偏向内侧比例 49% / 67%） |
| 航段起步阶段 | 偏差也大（0.15~0.20）——起步追赶瞬态 |
| 返程高度 | 系统偏高 **+0.039m**（0~+0.12），无害 |

### 调点规律（三条）

1. **直线段不用留余量**（横偏≈0）→ 可以贴到几何极限。
2. **弯道要额外留 0.15~0.2m**（飞机会往内侧切）。内侧有柱子时：
   - 把该弯的点整体往**外侧**挪 0.15，**或者**
   - **把弯做缓/做直**（在弯两侧加拉直点）——通常更有效
3. **每个航段起步后的第一个弯最危险**（起步瞬态 + 切内角叠加）。

### 注意"几何上限"（挪不动的）

例：case1 返程过 A左(2.7,1.55) 时，A左(x=2.7) 与东边界(x=4.0) 之间
**最优通过线是 x=3.40，净距上限只有 0.60** —— 只剩零点几可挪，
这种情况**只能靠"做直"（消除切内角）而不是"再往外挪"**。

---

## 四、返程独立化

**原来**：yaml 里没有返程途经点，`buildReturnVia()` 用 **leg2 倒放 + 穿环走廊** 拼出来
→ 编辑器改不到返程。

**现在**：新增 `map/via_return_case0~3`（返程绕柱段，**末点必须=悬停点**）。

- `buildReturnVia()` 优先用它；**缺失/点数<2 时自动回退**"leg2 倒放"旧行为
- 过孔走廊（悬停点→过孔→起飞点）仍是代码内置，不可编辑
- 初值 = 原 leg2 倒放 → 改动前后飞行行为完全一致（净距数值逐个核对过）

---

## 五、工具链

| 工具 | 作用 |
|---|---|
| `raicom_vision_laser/tools/traverse_check.py` | **计划值**离线校验：leg1 + 4 case 整段样条 + 返程直通 + case1/2 两段平滑。改完点必跑，不用 ROS |
| `raicom_vision_laser/scripts/trajectory_editor.py` | 图形编辑器：三分支独立（灰=leg1 / 蓝+红=leg2 去程 / 橙+紫=返程）、左键拖 / 右键点删 / 右键线插点 |
| `main_control` 任务结束日志 | **实测值**：任务计时上方打印"最近柱距"（到柱面）+ 位置，以及全场最紧与是否侵入膨胀圈 |
| `main_control` 跟踪日志 | 0.2s 一条，含 `设定/当前/前馈v/w/ff`，用于看滞后与权重 |

### 编辑器使用要点

- 三条路径数据**互相独立**，改一条不影响另一条
- 两套点重合时**紫点在上**（默认抓到返程）；**按住 Shift** 可抓蓝/灰点
- 锚点不可删：leg1 首末（出生点/悬停点）、leg2 首末（悬停点/投货区）、返程末点
- **右键点 = 删该点，右键线 = 在两点中点插点**；写回保留注释、行尾注释跟着坐标走
- 性能：背景只在切 case 时画一次、点批量一次 plot、拖动限频 —— 修掉了"拖两个点就卡死"
  （原来全量重绘 55ms/次 × 每次鼠标移动）

### ⚠️ 已修的写回 bug（2026-09-17）

现象：**点一次右键就把 `via_return_case1` 写乱成"相邻重复点 + 末尾丢点"，随后样条除零崩溃**，
且崩溃前已多次 `save()` 把文件写脏（看起来像"整个列表被删了"）。

两个根因（都在 `_sub_list`）：

1. `del lines[rows[drop_i]]` 之后，`rows` 里**后面的行号没有同步 −1** → 写值整体错位一行
   → 相邻两行写入同一个坐标 + 末尾丢一个点 → **重复点让样条弦长 h=0 → 除零**。
2. 逐行扫描时**遇到列表中间的独立注释行就停止**，于是"只写入了前几行 + 把剩余点当新行追加"
   → 列表变长、出现重复。

修复：

- 删行后行号同步 −1；行扫描改为「跳过块内注释/空行但继续」
- 删除改成"精确删那一行"（`_sub_list(..., drop_i)`），行尾注释跟着坐标走
- `polyline()` 加去重 + `try/except`：坏数据不再崩，退回按直线段画并在状态栏告警
- 插点防退化：新点与相邻点 < 0.04m 直接拒绝

**教训：不要把独立注释行放在 yaml 列表中间**（说明性注释放到 `key:` 之前）。
文件被写乱时可直接恢复：

```bash
git -C src/raicom_vision_laser checkout -- config/traverse_map.yaml
```

---

## 六、当前状态与提交

| 仓库 | 分支 | 提交 |
|---|---|---|
| `raicom_vision_laser` | `new_dev_vel_pos` | `17b7177` 分支基线（v_max 回退 0.5）<br>`41250d6` 独立返程途经点 + 编辑器重写<br>`058b635` 编辑器写回 bug 修复 + 返程 case1 过 A左 段做直 |
| `raicom26_ws` | `new_dev_vel_pos` | `6299ade` 速度前馈（position+velocity）<br>`6bab923` 曲率自适应 + 返程独立化<br>`548b5f4` 任务结束"最近柱距"诊断 |
| `raicom_vision_laser` | `master` | `7058d13` 折线航点 + 折线编辑器（路线 A，备用） |
| `raicom26_ws` | `master` | `651a75b` 折线接入主控（路线 A，备用） |

两个仓库的 `new_dev_vel_pos` 工作区均已提交、干净。本文件（`new_dev_vel_pos_notes.md`）
按约定留在本地不提交。

**关键参数现状**（`new_dev_vel_pos` 分支上的实际值）：
- `traverse_map.yaml`：`v_max 0.5 / a_max 0.4 / a_lat_max 0.6`、`inflation 0.33`、`sample_ds 0.01`
- `main_control.yaml`：`max_speed 0.8`、`p_xy 0.4`、`p_z 0.8`、`err_max 0.25`、
  `ff_straight 0.8`、`ff_curve 0.6`、`ff_horizon 0.10`

> ⚠️ `p_xy_drop`（投货区专用增益）与 `wp_drop_area_y = -2.10`（投货点对齐）是 **master 侧**的改动，
> **本分支没有**（本分支 `wp_drop_area_y` 仍是 -2.0）。两分支后续合并时注意别丢。

---

## 七、待办 / 未探明

- [ ] **实机验证**曲率自适应前馈 + 新返程点（case1 返程过 A左 计划 0.596，
      实测预计 ~0.50，比原来的 0.448 略好）
- [ ] 用新增的"任务结束最近柱距"日志长期收集实测净距，反推跟踪损失的统计规律
- [ ] 速度上限受 PX4 `MPC_XY_VEL_MAX` / `MPC_XY_ACC_MAX` 限制，**未探明当前值**；
      想真正提速需要动飞控参数（不在代码/yaml 里）
- [ ] case0 / case2 / case3 的返程尚未按"弯道切内角"规律优化过
      （case1 已做：过 A左 段拉直，见 `via_return_case1`）
- [ ] `case1/case2` 的"两段平滑"（`via_go_a/b`、`via_ret_a/b`）实际很少生效：
      边飞边扫走的 `tryPlanLeg2FromCurrent` **没有两段平滑分支**，只有"飞到悬停点才扫到 case"
      时才会走它 —— 是否统一待定
- [ ] 路线 A（折线）仍留在 master，若速度前馈路线走不通可回退

### 已完成（本轮）

- [x] 曲率自适应速度前馈（直线 ff 0.8 / 弯道 0.6），替代均匀 ff_scale
- [x] z 前馈过环门控修复（返程砸地弹跳）
- [x] 返程途经点独立化 `via_return_caseX` + 编辑器三分支独立编辑
- [x] 编辑器写回 bug（重复点/丢点/除零崩溃）修复 + 性能修复
- [x] `traverse_check.py` 补上 case1/2 整段样条的校验盲区
- [x] 任务结束打印实测"最近柱距"
